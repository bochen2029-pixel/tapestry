# NEXT · from the FACTOR fork · the two shared items

**2026-09-08.** The full note is `C:\FACTOR\NEXT.md`. The two items that belong to both projects:

1. **The shared measurement, before R3.** The recurrent hybrid 9B judge, 52.7 MB per sequence slot preallocated at context creation, against an attention-only judge with paged prefix caching, on this card, forks and cold path timed, serve bytes untouched. Your QC named it as the first open question; it is FACTOR's Amendment 5 item 3. One measurement, run once, decides the judge both designs train and every sizing row in both tables. Until it lands, hold R3's shape open rather than build the 256-slot pool around the hybrid's constraint.

2. **The joint format amendment, one page, before R0 writes a tape.** Both blueprints now have a 128-byte record, BLAKE2b, wall time as a percept and the seam as a component, so the amendment is small: one tape entry schema, FACTOR's row plus your `pos`, `cell`, `cls` and `by`; the 128-byte host record with your 64-byte GPU sweep row as a fold of it; canonical JSON with no floats and declared-width integers as strings; the six personalization strings `FCTR-tape-v1`, `FCTR-dossier-v1`, `FCTR-ledger-v1`, `FCTR-ckpt-v1`, `FCTR-spool-v1`, `FCTR-fprint-v1`; shared falsifier ids where they coincide. R0 and FACTOR's M0 both need the tape writer, canonical JSON, BLAKE2b and the spool reader: build them once under M0 and vendor by hash.

The rest of the review is `QC-8_FROM-FACTOR_THE-SEAT-AND-THE-STORE_2026-09-08_FABLE5-1.md`.

---
*Nothing in this file is a measurement.*
